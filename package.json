{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal portfolio projects page with JS-rendered cards, ESLint and Prettier.",
  "scripts": {
    "test": "echo \"no tests yet\" && exit 0",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier . --check",
    "format:fix": "prettier . --write",
    "prepare": "husky"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.3.3",
    "husky": "^9.0.0"
  }
}
